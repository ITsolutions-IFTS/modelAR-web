# Sketchfab API key — https://sketchfab.com/settings/password
VITE_SKETCHFAB_API_KEY=

# (Opcional) URL base del backend propio cuando esté disponible.
# Si se setea, el cliente llama a tu backend en lugar de Sketchfab directamente.
# VITE_API_BASE_URL=
